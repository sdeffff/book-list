export interface bookModel {
    title: string,
    author: string,
    category: string,
    ISBN: string,
    createdAt: string,
    editedAt: string,
    activated: boolean,
    id: string
}